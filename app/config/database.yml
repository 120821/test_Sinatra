default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: 5
  username: 'admin'
  password: '88888888'
  host: localhost

development:
  <<: *default
  database: datasetter_development

test:
  <<: *default
  database: datasetter_test

